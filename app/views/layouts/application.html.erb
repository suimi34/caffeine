<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "カフェイントラッカー" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="カフェイントラッカー">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <!-- ナビゲーションヘッダー -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
      <nav class="max-w-4xl mx-auto px-4">
        <div class="flex items-center justify-between h-14">
          <!-- ロゴ -->
          <%= link_to root_path, class: "flex items-center gap-2 text-emerald-600 font-bold text-lg" do %>
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
            <span class="hidden sm:inline">カフェイントラッカー</span>
          <% end %>

          <!-- ナビゲーションリンク -->
          <div class="flex items-center gap-1 sm:gap-4">
            <%= link_to root_path, class: "flex items-center gap-1 px-3 py-2 rounded-lg text-sm #{controller_name == 'dashboard' ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600 hover:bg-gray-100'}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
              </svg>
              <span class="hidden sm:inline">ホーム</span>
            <% end %>

            <%= link_to caffeine_intakes_path, class: "flex items-center gap-1 px-3 py-2 rounded-lg text-sm #{controller_name == 'caffeine_intakes' ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600 hover:bg-gray-100'}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"></path>
              </svg>
              <span class="hidden sm:inline">記録</span>
            <% end %>

            <%= link_to beverages_path, class: "flex items-center gap-1 px-3 py-2 rounded-lg text-sm #{controller_name == 'beverages' ? 'bg-emerald-50 text-emerald-700' : 'text-gray-600 hover:bg-gray-100'}" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
              </svg>
              <span class="hidden sm:inline">飲み物</span>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <main class="pb-8">
      <%= yield %>
    </main>
  </body>
</html>
