<div class="max-w-lg mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">摂取記録詳細</h1>

  <div class="bg-white shadow-md rounded-lg p-6">
    <dl class="space-y-4">
      <div>
        <dt class="text-sm font-medium text-gray-500">飲み物</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= @caffeine_intake.beverage.name %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">摂取日時</dt>
        <dd class="mt-1 text-lg text-gray-900"><%= l @caffeine_intake.consumed_at, format: :long %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">数量</dt>
        <dd class="mt-1 text-lg text-gray-900">× <%= @caffeine_intake.quantity %></dd>
      </div>
      <div>
        <dt class="text-sm font-medium text-gray-500">合計カフェイン量</dt>
        <dd class="mt-1 text-lg font-bold text-emerald-600"><%= @caffeine_intake.total_caffeine_mg %> mg</dd>
      </div>
    </dl>
  </div>

  <div class="mt-6 flex gap-4">
    <%= link_to "編集", edit_caffeine_intake_path(@caffeine_intake), class: "bg-indigo-600 hover:bg-indigo-700 text-white font-medium py-2 px-4 rounded-lg transition" %>
    <%= link_to "一覧に戻る", caffeine_intakes_path, class: "text-gray-600 hover:text-gray-900" %>
  </div>
</div>
