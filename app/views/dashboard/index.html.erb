<div class="max-w-4xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-gray-900 mb-8 text-center">カフェイン摂取トラッカー</h1>

  <% if notice %>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
      <%= notice %>
    </div>
  <% end %>

  <!-- 今日のサマリー -->
  <div class="bg-gradient-to-r from-emerald-500 to-teal-600 rounded-xl shadow-lg p-6 mb-8 text-white">
    <div class="flex items-center justify-between">
      <div>
        <p class="text-emerald-100 text-sm">今日のカフェイン摂取量</p>
        <p class="text-4xl font-bold mt-1"><%= @today_total %> mg</p>
        <p class="text-emerald-100 text-sm mt-2"><%= l @today, format: :long %></p>
      </div>
      <div class="text-right">
        <%= link_to new_caffeine_intake_path, class: "inline-flex items-center justify-center w-14 h-14 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-full transition" do %>
          <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
          </svg>
        <% end %>
      </div>
    </div>
  </div>

  <div class="grid md:grid-cols-2 gap-6 mb-8">
    <!-- 今日の摂取記録 -->
    <div class="bg-white rounded-xl shadow-md p-6">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-lg font-semibold text-gray-900">今日の記録</h2>
        <%= link_to "すべて見る →", caffeine_intakes_path, class: "text-sm text-emerald-600 hover:text-emerald-800" %>
      </div>

      <% if @today_intakes.any? %>
        <ul class="space-y-3">
          <% @today_intakes.each do |intake| %>
            <li class="flex items-center justify-between py-2 border-b border-gray-100 last:border-0">
              <div class="flex items-center gap-3">
                <span class="text-sm text-gray-500"><%= intake.consumed_at.strftime("%H:%M") %></span>
                <span class="font-medium text-gray-900"><%= intake.beverage.name %></span>
                <% if intake.quantity > 1 %>
                  <span class="text-xs text-gray-500">× <%= intake.quantity %></span>
                <% end %>
              </div>
              <span class="text-sm font-medium text-emerald-600"><%= intake.total_caffeine_mg %> mg</span>
            </li>
          <% end %>
        </ul>
      <% else %>
        <p class="text-gray-500 text-sm text-center py-4">今日はまだ記録がありません</p>
      <% end %>

      <div class="mt-4">
        <%= link_to new_caffeine_intake_path, class: "w-full block text-center bg-emerald-50 hover:bg-emerald-100 text-emerald-700 font-medium py-2 px-4 rounded-lg transition" do %>
          + 記録を追加
        <% end %>
      </div>
    </div>

    <!-- クイックアクセス：飲み物マスター -->
    <div class="bg-white rounded-xl shadow-md p-6">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-lg font-semibold text-gray-900">飲み物マスター</h2>
        <%= link_to "管理 →", beverages_path, class: "text-sm text-emerald-600 hover:text-emerald-800" %>
      </div>

      <% if @beverages.any? %>
        <ul class="space-y-3">
          <% @beverages.each do |beverage| %>
            <li class="flex items-center justify-between py-2 border-b border-gray-100 last:border-0">
              <span class="font-medium text-gray-900"><%= beverage.name %></span>
              <span class="text-sm text-gray-500"><%= beverage.caffeine_mg %> mg</span>
            </li>
          <% end %>
        </ul>
        <% if Beverage.count > 5 %>
          <p class="text-gray-400 text-sm text-center mt-2">他 <%= Beverage.count - 5 %> 件...</p>
        <% end %>
      <% else %>
        <p class="text-gray-500 text-sm text-center py-4">飲み物が登録されていません</p>
      <% end %>

      <div class="mt-4">
        <%= link_to new_beverage_path, class: "w-full block text-center bg-gray-50 hover:bg-gray-100 text-gray-700 font-medium py-2 px-4 rounded-lg transition" do %>
          + 飲み物を登録
        <% end %>
      </div>
    </div>
  </div>

  <!-- 情報 -->
  <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 text-sm text-amber-800">
    <p class="font-medium mb-1">カフェインの摂取目安</p>
    <p>健康な成人の場合、1日のカフェイン摂取量は400mg以下が推奨されています。</p>
  </div>
</div>
